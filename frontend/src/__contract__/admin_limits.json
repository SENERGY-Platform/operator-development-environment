{
  "currency": "EUR",
  "declared": {
    "kernel_cpu_default": "set on the KubeSpawner profile at spawn time, not by ODE: the Hub API selects a profile rather than carrying resource overrides",
    "kernel_cpu_max": "set on the KubeSpawner profile at spawn time, not by ODE: the Hub API selects a profile rather than carrying resource overrides",
    "kernel_mem_default": "set on the KubeSpawner profile at spawn time, not by ODE: the Hub API selects a profile rather than carrying resource overrides",
    "kernel_mem_max": "set on the KubeSpawner profile at spawn time, not by ODE: the Hub API selects a profile rather than carrying resource overrides",
    "max_concurrent_ray_jobs": "M7 (experiments/, SPEC §5.12)"
  },
  "defaults": {
    "max_tier": "L2",
    "period": "720h0m0s",
    "soft_warn_fraction": 0.8
  },
  "enforced": [
    "period",
    "token_cap",
    "cost_cap",
    "soft_warn_fraction",
    "global_cost_cap",
    "allowed_providers",
    "allowed_models",
    "max_tier",
    "max_concurrent_sessions"
  ],
  "limits": [],
  "pricing": []
}
